need to add the app/root layout template so the livesocket js gets executed
  https://hexdocs.pm/phoenix_live_view/live-layouts.html
  likely just need to generate a new phoenix liveview app and see how this is done
